{
  "name": "tiliger-multirepo",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "boot": "lerna bootstrap",
    "dev": "lerna run --parallel dev",
    "test": "lerna run --parallel test",
    "lint": "lerna run --parallel lint:fix",
    "build": "lerna --parallel build"
  },
  "dependencies": {}
}
